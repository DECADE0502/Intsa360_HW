# Synthetic BOM semantic fixtures

These fixtures use fictional board names, material codes, descriptions and
references. They model workbook structure only.

Real company BOM files must not be copied into this directory. Local golden
tests read the private sample directory from
`HWAGENT_BOM_COMPARE_SAMPLES_ROOT` and are skipped when it is absent.
